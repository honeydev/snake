define(function() {

    'use strict';

    return class TimerTest extends BaseTest {

        constructor(mocha, chai, timer) {
            super(mocha, chai);
            this._timer = timer;
        };

        testTimer() {
           	
        };
    };
});