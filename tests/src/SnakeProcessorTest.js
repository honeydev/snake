define(['app/SnakeProcessor'], function(mocha, chai, BaseTest, snakeProcessor) {

    class SnakeProcessorTest {
        
        constructor(mocha, chai, snakeProcessor) {
            
        };
    }

    return new SnakeProcessor(mocha, chai, snakeProcessor);
});