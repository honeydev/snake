define(function() {

    'use strict';

    return class MessageProcessor {
        /**
         * @param {object} with messages for object
         */
        processMessage(message) {

        };
    };
});